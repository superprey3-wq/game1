(()=>{const app=document.getElementById('app'),game=document.getElementById('game');if(!app||!game)return;const c=document.createElement('canvas');c.style.cssText='position:fixed;inset:0;width:100%;height:100%;pointer-events:none;z-index:3';app.appendChild(c);const x=c.getContext('2d');let W=0,H=0,dpr=1,t=0;const img=new Image();img.src='assets/v16/lumen-wolf.svg';const motes=Array.from({length:70},()=>({x:Math.random(),y:Math.random(),s:.4+Math.random()*1.4,v:.06+Math.random()*.14,p:Math.random()*6.28}));const wisps=Array.from({length:8},()=>({x:Math.random(),y:Math.random(),r:80+Math.random()*130,p:Math.random()*6.28}));function rs(){W=innerWidth;H=innerHeight;dpr=Math.min(devicePixelRatio||1,2);c.width=W*dpr;c.height=H*dpr;x.setTransform(dpr,0,0,dpr,0,0)}rs();addEventListener('resize',rs);function draw(){t+=.016;x.clearRect(0,0,W,H);for(const w of wisps){let px=(w.x*W+Math.sin(t*.18+w.p)*80)%W,py=(w.y*H+Math.cos(t*.14+w.p)*45)%H;let g=x.createRadialGradient(px,py,0,px,py,w.r);g.addColorStop(0,'rgba(87,216,255,.055)');g.addColorStop(1,'rgba(87,216,255,0)');x.fillStyle=g;x.beginPath();x.arc(px,py,w.r,0,Math.PI*2);x.fill()}for(const m of motes){let px=(m.x*W+t*m.v*70)%W,py=m.y*H+Math.sin(t*1.2+m.p)*12;x.globalAlpha=.18+.22*Math.sin(t*2+m.p)**2;x.fillStyle=m.s>1?'#bca4ff':'#9ef5ff';x.beginPath();x.arc(px,py,m.s,0,Math.PI*2);x.fill()}x.globalAlpha=1;const vign=x.createRadialGradient(W/2,H/2,Math.min(W,H)*.28,W/2,H/2,Math.max(W,H)*.72);vign.addColorStop(0,'rgba(0,0,0,0)');vign.addColorStop(1,'rgba(2,10,18,.22)');x.fillStyle=vign;x.fillRect(0,0,W,H);const playing=!document.getElementById('hud')?.classList.contains('hidden');if(playing&&img.complete){const wx=W/2-72+Math.cos(t*1.6)*14,wy=H/2+58+Math.sin(t*2.1)*6;x.save();x.globalAlpha=.92;x.shadowBlur=18;x.shadowColor='#67eaff';x.drawImage(img,wx-54,wy-36,108,72);x.restore();x.fillStyle='rgba(118,238,255,.18)';x.beginPath();x.ellipse(wx,wy+25,42,9,0,0,Math.PI*2);x.fill()}requestAnimationFrame(draw)}draw()})();